# Dependencies
node_modules/

# Build output
dist/
.astro/

# Raw sprite-pipeline input (5.7 GB — never commit; rebuilt via scripts/).
sprite-source/

# Animated Pokémon webps are hosted on the GCS bucket, not committed. They are
# generated locally by scripts/optimize-sprites.mjs and pushed with
# scripts/upload-sprites.mjs. Item icons + the box-icon sheet stay tracked.
public/sprites/pokemon/

# Logs and OS cruft
*.log
.DS_Store

# Local env
.env
.env.*
