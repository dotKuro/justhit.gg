# Keep the Docker build context small and the build reproducible.
# sprite-source/ is the 5.7 GB raw sprite input — it is NOT needed to build the
# site (the optimized sprites already live in public/sprites/).
sprite-source

# Pokémon webps and item renders are served from the GCS bucket, not the image.
public/sprites/pokemon
public/sprites/items

node_modules
dist
.astro
.git
.gitignore
*.log
.DS_Store
